version: '3'

services:
  loadtesting:
    build:
      context: .
      dockerfile: ./build/python/Dockerfile
    command: bash -c "echo 'To run load test, use docker/docker-compose exec to invoke Python manually.' && tail -f /dev/null"
    networks:
      - loadtesting
      - hospital-discovery

networks:
  loadtesting:
    external:
      name: hrms-loadtesting
  hospital-discovery:
    external:
      name: hrms-hospital-discovery
